version: "3"

services:
  db:
    image: postgres
    volumes:
      - ./db_init:/docker-entrypoint-initdb.d
    environment:
      POSTGRES_USER: albertcito
      POSTGRES_DB: albertcito
      POSTGRES_PASSWORD: 1234
    ports:
      - 5433:5432